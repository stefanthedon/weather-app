import React from "react";

const Title = () => (
	<div>
		<h1 className="title-container__title">Weather Locator</h1>
		<p className="title-container__subtitle">Find out localized weather info...</p>
	</div>
);

export default Title;